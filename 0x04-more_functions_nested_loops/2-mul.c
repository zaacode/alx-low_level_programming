#include "main.h"
/**
 * mul - a function that multiplies tow integers
 * @a: the first integer
 * @b: the second integer
 * Return: the product of the two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
